<Window x:Class="MyMoviesWPF.MVVM.View.Catalog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:MyMoviesWPF.MVVM.View" 
        mc:Ignorable="d"
        Title="Каталог" Height="900" Width="1600"        
        WindowStartupLocation="CenterScreen">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="31*"/>
            <RowDefinition Height="190*"/>
        </Grid.RowDefinitions>

        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="139*"/>
            <ColumnDefinition Width="485*"/>
            <ColumnDefinition Width="176*"/>
        </Grid.ColumnDefinitions>

        <StackPanel Grid.Column="1" Orientation="Horizontal" HorizontalAlignment="Center" Margin="0,104,0,0" Grid.RowSpan="2" Width="972">
            <DataGrid Height="350" Width="972"/>
        </StackPanel>

        <ContentControl 
                HorizontalAlignment="Center" VerticalAlignment="Top" Grid.Row="0" Grid.Column="2"
                Content="{Binding CurrentControlView}" />
    </Grid>
</Window>
